# @uwu/flora-cli

npm wrapper for the `flora` Rust CLI.

Install globally:

```bash
npm i -g @uwu/flora-cli
```

The package downloads a platform-specific `flora` binary from GitHub Releases during install.
