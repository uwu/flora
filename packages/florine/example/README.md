# Florine example

This example renders the local `@flora/florine` package in a Vite React app.

## Development

From `packages/florine`, run:

```bash
vp run dev:example
```

Build the package and example with:

```bash
vp run build:example
```

Check the example with:

```bash
vp run check:example
```
