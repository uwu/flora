{
  "name": "@flora-monorepo/flora",
  "private": true,
  "scripts": {
    "prepare": "vp config",
    "format": "cargo fmt && vp fmt",
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r"
  },
  "devDependencies": {
    "@uwu/flora-cli": "workspace:*",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
